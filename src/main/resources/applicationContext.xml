<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aws-context="http://www.springframework.org/schema/cloud/aws/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/cloud/aws/context">

    <!-- Enable EC2 instance metadata gathering support -->
    <!--<aws-context:context-instance-data />-->

    <!-- Setup credentials for accessing DynamoDB -->
    <aws-context:context-credentials>
        <!--<aws-context:instance-profile-credentials/> -->
        <aws-context:simple-credentials access-key="${accessKey:}"
                                        secret-key="${secretKey:}"/>
    </aws-context:context-credentials>

    <!-- Setup EC2 region -->
    <!--<aws-context:context-region auto-detect="true" />-->
    <!--<aws-context:context-region region="${cloud.aws.region.static:}" />-->
</beans>